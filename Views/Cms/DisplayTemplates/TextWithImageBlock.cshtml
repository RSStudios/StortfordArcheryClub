@model StortfordArchers.Blocks.TextWithImageBlock

<div class="row">
    <div class="col-2">
        @if (Model.ImgBody.HasValue)
        {
            <div style="width: 100px;">
                <img src="@Url.Content(Model.ImgBody)" />
            </div>
        }
    </div>

    <div class="col-10">
        @Html.Raw(Model.HtmlBody)
    </div>
</div>